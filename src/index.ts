import Epub from './epub.js';
import type {
  CoverType,
  Metadata,
  Options,
  Resource,
  Section,
} from './types.js';

/* eslint-disable import/no-unused-modules */
export default Epub;
export type { CoverType, Metadata, Options, Resource, Section };
